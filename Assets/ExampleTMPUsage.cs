/*using UnityEngine;

public class ExampleTMPUsage : MonoBehaviour
{
    public TMPPopupManager tmpPopupManager;

    void Start()
    {
        // Example usage
        //tmpPopupManager.CreateTextPopup(new Vector3(0, 0, 0), "Hello, World!");
    }

    void Update()
    {
        if (Input.GetKeyDown(KeyCode.Space))
        {
            //tmpPopupManager.CreateTextPopup(transform.position, "Space key pressed!");
        }
    }
}
*/