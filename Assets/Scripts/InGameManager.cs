using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    /* public GameObject boyCharacterGame; // Character in the game
     public GameObject girlCharacterGame; // Character in the game
     public CameraFollow cameraFollow; // Reference to the CameraFollow script
     private GameObject selectedCharacter;
     private Transform selectedCharacterTransform;
     */
    GameStartManager gameStartManager;
    void Start()
    {
       // gameStartManager.LoadSelectedCharacter();
    }
   

    // Update is called once per frame
    void Update()
    {
        
    }
}
